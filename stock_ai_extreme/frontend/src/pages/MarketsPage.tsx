import MarketsOverview from "../components/MarketsOverview";

export default function MarketsPage(){
  return <main>
    <header><p>Crypto, commodities, and forex — same real data pipeline as everything else</p><h1>Markets</h1></header>
    <MarketsOverview/>
  </main>;
}